

## Plan: Brighten Starfield

In `VehicleShowroom3D.tsx`, increase the `PointMaterial` properties in the `Starfield` component:

- Increase `size` from `0.035` to `0.055`
- Increase `opacity` from `0.8` to `1.0`

This will make the stars more visible and vibrant against the dark backdrop.

